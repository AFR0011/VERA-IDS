# Data Manifest

No dataset file is tracked. A local full-run manifest must record dataset name,
official URL, acquisition date, provider terms, archive and extracted-file
SHA-256 hashes, schema, row/column counts, taxonomy mapping, preprocessing code
commit/config hash, and split counts. Store it with local data or an approved
archive, not by adding benchmark rows to Git.
