# Figures

The Protocol A trade-off, delta, and leaderboard figures were regenerated for
`v2026.08` from the supported-label primary metric using
`scripts/regenerate_release_figures.py`. The per-family heatmap is unaffected by
the system-level denominator correction.

Source tables are under `outputs/summaries/`; metric definitions and the evidence
hash are in `PROTOCOL_A_CORRECTION.md`. Figures are CC BY 4.0.
