"""Native, package-local implementations of the published experiment lanes.

These modules preserve the author-owned phase implementations while replacing
the former dependency on scripts outside the public repository.
"""
